# Single source of truth for Xboard branding and SMTP settings.
# Copy to .runtime/xboard/shared.env and edit that runtime file.

XBOARD_APP_NAME=Capybara
XBOARD_MAIL_HOST=smtp.example.com
XBOARD_MAIL_PORT=587
XBOARD_MAIL_ENCRYPTION=tls
XBOARD_MAIL_USERNAME=
XBOARD_MAIL_PASSWORD=
XBOARD_MAIL_FROM_ADDRESS=
