import 'package:flutter/widgets.dart';

class WebCrispWidget extends StatelessWidget {
  const WebCrispWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
