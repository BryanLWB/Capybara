name: app_api
description: Neutral app-facing API layer for the Capybara client.
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  http: ^1.2.0
  logging: ^1.2.0
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  redis_dart_client: ^1.0.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
